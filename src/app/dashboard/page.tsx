import Button from '@/components/ui/Button'
import React, { FC } from 'react'

interface pageProps {}

const page:FC<pageProps> = ({}) => {
  return (
    <Button>
      Hello
    </Button>
  )
}

export default page